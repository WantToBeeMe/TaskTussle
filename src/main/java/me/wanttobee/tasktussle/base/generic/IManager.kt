package me.wanttobee.tasktussle.base.generic

import org.bukkit.Material

abstract class IManager(val iconMaterial: Material, val subjectName : String, val iconDescription: String) {
    // making sure the all managers at least have these 3 attributes
}